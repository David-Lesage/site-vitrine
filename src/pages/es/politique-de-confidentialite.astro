---
// Le slug reste `politique-de-confidentialite` : c'est la convention du site
// (cf. /es/mentions-legales, /es/conditions-generales) — les routes ne sont pas
// traduites, seul le contenu l'est. `localizePath('/politique-de-confidentialite',
// 'es')` produit donc exactement cette URL, et c'est celle que le pied de page sert.
import PrivacyPage from "@/components/pages/PrivacyPage.astro"
---

<PrivacyPage lang="es" />
